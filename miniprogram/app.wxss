/** 全局样式 - TableSync 微信小程序（iPhone 安全区 + 滚动优化） */
page {
  /* Brand palette: TableSync 故事感插画色盘 */
  --bg: #FDF9EF;
  --bg-soft: #FAF9F7;
  --bg-warm: #F5EDE3;
  --bg-contrast: #FFFFFF;
  --card-bg: #FAF9F7;
  --card-bg-strong: #FFFFFF;

  --text: #2D2D2D;
  --text-soft: #5A544F;
  --text-muted: #8E857E;
  --text-subtle: #A09A94;
  --text-inverse: #FFFFFF;

  --accent: #E84A27;
  --accent-strong: #D4421F;
  --accent-soft: rgba(232, 74, 39, 0.12);
  --accent-soft-2: rgba(232, 74, 39, 0.08);
  --accent-border: rgba(232, 74, 39, 0.35);
  --accent-border-strong: rgba(232, 74, 39, 0.35);

  --line: #E5EBE6;
  --line-strong: #E8E4E0;

  --ink-green: #1B3D2F;
  --mustard: #D9A62E;
  --warm-glow: #FFF8E8;

  --shadow: rgba(45, 45, 45, 0.08);
  --shadow-strong: rgba(45, 45, 45, 0.12);

  --info: #4A5D6E;
  --info-soft: rgba(100, 149, 237, 0.12);
  --info-border: rgba(100, 149, 237, 0.6);

  --safe-bottom: env(safe-area-inset-bottom);
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', sans-serif;
  background-color: var(--bg);
  color: var(--text);
  box-sizing: border-box;
  height: 100%;
  min-height: 100%;
}

view, text, scroll-view, button, input, textarea, image, navigator {
  box-sizing: border-box;
}

/* 图片视口适配：高度不固定 px，用 vh + 不拉伸 */
image {
  max-height: 30vh;
  object-fit: cover;
}

/* 可滚动容器：iOS 顺滑滚动 */
.scroll-container {
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  height: 100%;
}
