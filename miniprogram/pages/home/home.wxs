/**
 * 修复回滚后缺失的辅助脚本
 */
var includes = function(array, value) {
  if (!array || !value) return false;
  return array.indexOf(value) !== -1;
};

// 检查某个值是否在数组中，用于忌口按钮的高亮显示
module.exports = {
  includes: includes
};