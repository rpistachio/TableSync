<view class="scan-container">

  <!-- ═══ 顶部标题区 ═══ -->
  <view class="scan-header">
    <text class="scan-title">拍照清冰箱</text>
    <text class="scan-subtitle">拍一张或多张冰箱照片，AI 帮你搭配今晚菜谱</text>
  </view>

  <!-- ═══ 图片选择区（多图横向滑动）═══ -->
  <view class="photo-area">
    <!-- 有图片时：横向滑动图片列表 + 末尾加号 -->
    <scroll-view class="photo-list" scroll-x enhanced show-scrollbar="{{false}}" wx:if="{{imageList.length > 0}}">
      <view class="photo-list-inner">
        <view class="photo-thumb-wrap" wx:for="{{imageList}}" wx:key="path">
          <image class="photo-thumb" src="{{item.path}}" mode="aspectFill" />
          <view class="photo-thumb-remove" catchtap="onRemoveImage" data-index="{{index}}">
            <text class="photo-thumb-remove-icon">×</text>
          </view>
        </view>
        <!-- 末尾加号按钮（未满 5 张时显示）-->
        <view class="photo-thumb-add" bindtap="onChooseImage" wx:if="{{imageList.length < 5}}">
          <text class="photo-thumb-add-icon">+</text>
          <text class="photo-thumb-add-text">添加</text>
        </view>
      </view>
    </scroll-view>

    <!-- 无图片时：空状态大占位 -->
    <view class="photo-box" bindtap="onChooseImage" wx:else>
      <view class="photo-placeholder">
        <text class="photo-placeholder-icon">📷</text>
        <text class="photo-placeholder-text">拍照 / 从相册选择</text>
        <text class="photo-placeholder-hint">最多 5 张，建议拍摄冰箱打开后的全貌</text>
      </view>
    </view>

    <!-- 图片计数 -->
    <view class="photo-count" wx:if="{{imageList.length > 0}}">
      <text class="photo-count-text">已选 {{imageList.length}} / 5 张</text>
    </view>
  </view>

  <!-- ═══ 操作按钮区 ═══ -->
  <view class="action-bar" wx:if="{{stage === 'idle' && imageList.length > 0}}">
    <button class="scan-btn" bindtap="onStartScan">
      <text class="scan-btn-text">开始识别</text>
    </button>
  </view>

  <!-- ═══ 加载状态 ═══ -->
  <view class="loading-area" wx:if="{{stage === 'uploading' || stage === 'scanning'}}">
    <view class="loading-ring"></view>
    <text class="loading-text">{{statusText}}</text>
    <view class="loading-steps">
      <view class="loading-step {{stage === 'uploading' || stage === 'scanning' ? 'is-done' : ''}}">
        <text class="step-dot">{{stage === 'scanning' ? '✓' : '●'}}</text>
        <text class="step-label">上传图片</text>
      </view>
      <view class="loading-step-line {{stage === 'scanning' ? 'is-active' : ''}}"></view>
      <view class="loading-step {{stage === 'scanning' ? 'is-active' : ''}}">
        <text class="step-dot">{{stage === 'scanning' ? '●' : '○'}}</text>
        <text class="step-label">AI 识别</text>
      </view>
      <view class="loading-step-line"></view>
      <view class="loading-step">
        <text class="step-dot">○</text>
        <text class="step-label">组餐推荐</text>
      </view>
    </view>
  </view>

  <!-- ═══ 错误状态 ═══ -->
  <view class="error-area" wx:if="{{stage === 'error'}}">
    <text class="error-icon">😵</text>
    <text class="error-text">{{statusText}}</text>
    <button class="retry-btn" bindtap="onRetry">重新开始</button>
  </view>

  <!-- ═══ 食材确认/编辑区（Part 2a）═══ -->
  <view class="result-area" wx:if="{{stage === 'ingredients_review'}}">

    <!-- 食材编辑区 -->
    <view class="result-section">
      <view class="section-header">
        <text class="section-title">确认食材</text>
        <text class="section-badge" wx:if="{{ingredients.length > 0}}">{{ingredients.length}} 种</text>
      </view>
      <text class="section-hint">删除误识别的食材，或手动添加遗漏的食材</text>

      <!-- 可编辑的食材标签（带删除按钮）-->
      <view class="ingredient-tags" wx:if="{{ingredients.length > 0}}">
        <view class="ingredient-tag ingredient-tag--editable" wx:for="{{ingredients}}" wx:key="name">
          <text class="ingredient-icon">{{item.icon}}</text>
          <text class="ingredient-name">{{item.name}}</text>
          <text class="ingredient-qty" wx:if="{{item.quantity}}">{{item.quantity}}</text>
          <view class="ingredient-remove" catchtap="onRemoveIngredient" data-index="{{index}}">
            <text class="ingredient-remove-icon">×</text>
          </view>
        </view>
      </view>

      <!-- 无食材空状态 -->
      <view class="ingredients-empty" wx:if="{{ingredients.length === 0}}">
        <text class="ingredients-empty-icon">🔍</text>
        <text class="ingredients-empty-text">{{statusText || '暂无食材，请手动添加'}}</text>
      </view>

      <!-- 置信度 -->
      <view class="confidence-bar" wx:if="{{confidence > 0 && ingredients.length > 0}}">
        <text class="confidence-label">识别置信度</text>
        <view class="confidence-track">
          <view class="confidence-fill" style="width: {{confidence * 100}}%;"></view>
        </view>
        <text class="confidence-value">{{confidence > 0 ? (confidence * 100) + '%' : ''}}</text>
      </view>

      <!-- 备注 -->
      <view class="notes-tip" wx:if="{{notes}}">
        <text class="notes-icon">💡</text>
        <text class="notes-text">{{notes}}</text>
      </view>

      <!-- 手动添加食材 -->
      <view class="add-ingredient-area">
        <!-- 添加按钮 -->
        <view class="add-ingredient-btn" bindtap="onToggleAddInput" wx:if="{{!showAddInput}}">
          <text class="add-ingredient-btn-icon">+</text>
          <text class="add-ingredient-btn-text">手动添加食材</text>
        </view>
        <!-- 输入表单 -->
        <view class="add-ingredient-form" wx:if="{{showAddInput}}">
          <input
            class="add-ingredient-input"
            placeholder="输入食材名称，如：西红柿"
            value="{{newIngredientName}}"
            bindinput="onNewIngredientInput"
            bindconfirm="onAddIngredient"
            focus="{{showAddInput}}"
            maxlength="20"
          />
          <view class="add-ingredient-actions">
            <view class="add-ingredient-cancel" bindtap="onToggleAddInput">取消</view>
            <view class="add-ingredient-confirm" bindtap="onAddIngredient">添加</view>
          </view>
        </view>
      </view>
    </view>

    <!-- 底部确认操作 -->
    <view class="confirm-bar">
      <view class="timing-info" wx:if="{{totalMs > 0}}">
        <text class="timing-text">识别耗时 {{totalMs > 1000 ? (totalMs / 1000) + 's' : totalMs + 'ms'}}</text>
      </view>
      <button class="confirm-btn" bindtap="onConfirmIngredients" disabled="{{ingredients.length === 0}}">
        <text class="confirm-btn-text">开始推荐（{{ingredients.length}} 种食材）</text>
      </button>
      <button class="retry-btn-outline" bindtap="onRetry">重新扫描</button>
    </view>
  </view>

  <!-- ═══ 识别结果 + 交互选菜（Part 2b）═══ -->
  <view class="result-area" wx:if="{{stage === 'done'}}">

    <!-- 已选菜谱计数（置顶提示） -->
    <view class="selected-count-bar">
      <text class="selected-count-icon">🍽️</text>
      <text class="selected-count-text">已选 <text class="selected-count-num">{{selectedCount}}</text> 道菜</text>
    </view>

    <!-- 识别到的食材（只读摘要） -->
    <view class="result-section" wx:if="{{ingredients.length > 0}}">
      <view class="section-header">
        <text class="section-title">识别到的食材</text>
        <text class="section-badge">{{ingredients.length}} 种</text>
      </view>
      <view class="ingredient-tags">
        <view class="ingredient-tag" wx:for="{{ingredients}}" wx:key="name">
          <text class="ingredient-icon">{{item.icon}}</text>
          <text class="ingredient-name">{{item.name}}</text>
          <text class="ingredient-qty" wx:if="{{item.quantity}}">{{item.quantity}}</text>
        </view>
      </view>
    </view>

    <!-- AI 组餐推荐（可勾选） -->
    <view class="result-section" wx:if="{{recommendations.length > 0}}">
      <view class="section-header">
        <text class="section-title">今晚推荐</text>
        <text class="section-hint-inline">点击卡片可取消勾选</text>
      </view>

      <!-- 组餐摘要 -->
      <view class="meal-summary" wx:if="{{mealSummary}}">
        <text class="meal-summary-icon">👨‍🍳</text>
        <text class="meal-summary-text">{{mealSummary}}</text>
      </view>

      <!-- 推荐菜谱卡片（可勾选） -->
      <view class="recipe-cards">
        <view class="recipe-card {{item.selected ? 'is-selected' : 'is-unselected'}}"
              wx:for="{{recommendations}}" wx:key="id"
              bindtap="onToggleRecipe" data-index="{{index}}">
          <!-- 选中状态指示器 -->
          <view class="recipe-check {{item.selected ? 'is-checked' : ''}}">
            <text class="recipe-check-icon">{{item.selected ? '✓' : ''}}</text>
          </view>
          <image class="recipe-cover" src="{{item.coverUrl}}" mode="aspectFill" />
          <view class="recipe-info">
            <view class="recipe-top-row">
              <text class="recipe-name">{{item.name}}</text>
              <text class="recipe-role role-{{item.roleClass}}">{{item.roleLabel}}</text>
            </view>
            <text class="recipe-reason">{{item.reason}}</text>
            <view class="recipe-meta">
              <text class="recipe-time" wx:if="{{item.cook_minutes > 0}}">⏱ {{item.cook_minutes}}分钟</text>
              <view class="recipe-missing" wx:if="{{item.missing_ingredients.length > 0}}">
                <text class="missing-label">还需：</text>
                <text class="missing-item" wx:for="{{item.missing_ingredients}}" wx:for-item="mi" wx:key="*this">{{mi}}</text>
              </view>
            </view>
          </view>
        </view>
      </view>
    </view>

    <!-- 更多匹配菜谱（可勾选加入） -->
    <view class="result-section" wx:if="{{allMatched.length > 0}}">
      <view class="section-header" bindtap="onToggleMoreMatched">
        <text class="section-title">更多匹配菜谱</text>
        <text class="section-badge">{{allMatched.length}} 道</text>
        <text class="section-hint-inline" wx:if="{{!showMoreMatched}}">点击展开</text>
        <text class="section-arrow {{showMoreMatched ? 'is-up' : ''}}">▾</text>
      </view>
      <view class="more-matched-list {{showMoreMatched ? 'is-open' : ''}}">
        <view class="matched-item {{item.selected ? 'is-selected' : ''}}"
              wx:for="{{allMatched}}" wx:key="id"
              bindtap="onToggleMatchedRecipe" data-index="{{index}}">
          <!-- 勾选框 -->
          <view class="matched-check {{item.selected ? 'is-checked' : ''}}">
            <text class="matched-check-icon">{{item.selected ? '✓' : '+'}}</text>
          </view>
          <!-- 封面缩略图 -->
          <image class="matched-cover" src="{{item.coverUrl}}" mode="aspectFill" />
          <view class="matched-info">
            <view class="matched-name-row">
              <text class="matched-name">{{item.name}}</text>
              <text class="matched-meta-tag" wx:if="{{item.cook_type}}">{{item.cook_type}}</text>
            </view>
            <!-- 匹配度进度条 -->
            <view class="matched-detail">
              <view class="matched-score-bar">
                <view class="matched-score-fill" style="width: {{item.scorePercent}}%;"></view>
              </view>
              <text class="matched-score-inline">{{item.scorePercent}}%</text>
            </view>
            <!-- 已匹配 / 缺少食材概要 -->
            <view class="matched-ingredient-summary" wx:if="{{item.matchedIngredients.length > 0 || item.missingIngredients.length > 0}}">
              <text class="matched-have" wx:if="{{item.matchedIngredients.length > 0}}">✓ 已有{{item.matchedIngredients.length}}种</text>
              <text class="matched-need" wx:if="{{item.missingIngredients.length > 0}}">还需{{item.missingIngredients.length}}种</text>
            </view>
            <!-- 选中后展示缺少的具体食材 -->
            <view class="matched-missing-detail" wx:if="{{item.selected && item.missingIngredients.length > 0}}">
              <text class="matched-missing-label">需购买：</text>
              <text class="matched-missing-tag" wx:for="{{item.missingIngredients}}" wx:for-item="mi" wx:key="*this">{{mi}}</text>
            </view>
          </view>
        </view>
      </view>
    </view>

    <!-- 底部操作 -->
    <view class="result-bottom">
      <view class="timing-info" wx:if="{{totalMs > 0}}">
        <text class="timing-text">识别耗时 {{totalMs > 1000 ? (totalMs / 1000) + 's' : totalMs + 'ms'}}</text>
      </view>
      <button class="confirm-btn" bindtap="onConfirmRecipes" disabled="{{selectedCount === 0}}">
        <text class="confirm-btn-text">确认 {{selectedCount}} 道菜，查看购物清单</text>
      </button>
      <button class="retry-btn-outline" bindtap="onRetry">重新扫描</button>
    </view>
  </view>

</view>
