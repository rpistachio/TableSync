function includes(arr, val) {
  if (!arr || typeof arr !== 'object' || typeof arr.length !== 'number') return false;
  for (var i = 0; i < arr.length; i++) {
    if (arr[i] === val) return true;
  }
  return false;
}

module.exports = {
  includes: includes
};
